#include<iostream>
using namespace std;
int fibonachy(int x){
    if ((x==1) or (x==2)){
        return 1;
    }
    return fibonachy(x-1) + fibonachy(x-2);
}
int main(){
    cout << "VVedite a";
    int a;
    cin >> a;
    int res = fibonachy(a);
    cout << res;
}
